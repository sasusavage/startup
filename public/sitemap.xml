<?xml version="1.0" encoding="UTF-8"?>
<!--
  Only real, indexable pages. Fragment URLs (/#about) are deliberately absent:
  Google treats them as the same page and they add nothing but noise.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://sasusync.com/</loc>
    <lastmod>2026-08-17</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
  <!--
    sms.sasusync.com is deliberately absent. A sitemap should only list URLs
    on its own host, and a subdomain counts as a separate host. That service
    is a different application and needs its own sitemap; Google reaches it
    meanwhile via the link on the landing page.
  -->
</urlset>
